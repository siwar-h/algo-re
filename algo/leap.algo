FUNCTION LeapYear(year) : STRING
VAR
    year : INTEGER;
    leap : STRING := Leap year:;
    not : STRING:= Not a leap year:;
BEGIN
    IF ((year % 4 ==0 AND year % 4 !==0 ) OR year % 400 ==0) THEN
        RETURN  leap + year
    ELSE
        RETURN not + year;
    END_IF
       
END